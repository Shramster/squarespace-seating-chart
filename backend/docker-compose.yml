name: seatchart-ticketing

services:
  web:
    build: .
    command: sh /code/start.sh
    env_file:
      - .env
    volumes:
      - .:/code
      - ../dist:/dist
    ports:
      - "${PORT:-8100}:8000"
