SECRET_KEY=
SQUARESPACE_WEBHOOK_SECRET=
CORS_ALLOWED_ORIGINS=http://127.0.0.1:3000

# Only needed to run the `poller` service (see docker-compose.yml) — a
# site-level Commerce API key with Orders (Read Only) permission, used to
# poll for sales since Squarespace's push Webhook Subscriptions API is
# OAuth-only and can't be created with a plain API key.
SQUARESPACE_API_KEY=
